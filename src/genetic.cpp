#include <bits/stdc++.h>
#include <cstddef>
#include "../include/sbox.hpp"
#include <random>

namespace {
	std::mt19937 rng(std::chrono::steady_clock::now().time_since_epoch().count());
}


namespace genetic {

template<std::size_t N, std::size_t M>
	defs::SBox<N, M> mutateSBox() {

}

}
